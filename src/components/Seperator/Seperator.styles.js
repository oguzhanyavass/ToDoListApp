import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderWidth: 0.5,
    borderColor: '#78909C',
    marginLeft: 10,
    marginRight: 10,
  },
});

export {styles};
